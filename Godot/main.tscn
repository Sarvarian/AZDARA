[gd_scene load_steps=3 format=2]

[ext_resource path="res://lib/game.gdns" type="Script" id=1]

[sub_resource type="GDScript" id=1]
script/source = "extends Node2D


## VisualServer expects references to be kept around.
var sprite

func _ready():
	# Create a canvas item, child of this node.
	var ci_rid = VisualServer.canvas_item_create()
	# Make this node the parent.
	VisualServer.canvas_item_set_parent(ci_rid, get_canvas_item())
	# Draw a sprite on it.
	# Remember, keep this reference.
#	sprite = load(\"res://icon.png\")
	# Add it, centered.
#	VisualServer.canvas_item_add_texture_rect(ci_rid, Rect2(sprite.get_size() / 2, sprite.get_size()), sprite)

"

[node name="Main" type="Node"]

[node name="Rust" type="Node" parent="."]
script = ExtResource( 1 )

[node name="GDscript" type="Node2D" parent="."]
script = SubResource( 1 )
